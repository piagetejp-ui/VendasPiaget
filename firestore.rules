rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // TEMPORÁRIO: somente para desenvolvimento enquanto o Authentication não foi ativado.
    // Não publique o painel com esta regra.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
